# 全局feign配置

feign:
  client:
    config:
      default:  # default表示全部
        logger-level: Basic
  httpclient:
    enabled: true # 开启feign对httpClient依赖
    max-connections: 200
    max-connections-per-route: 50

logging:
  level:
    root: debug
